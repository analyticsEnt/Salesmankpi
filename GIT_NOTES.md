git commit -m "Initial commit - Salesman KPI dashboard"
git branch -M main
git remote add origin https://github.com/analyticsEnt/Salesmankpi.git
git push -u origin main

# After first push
git add .
git commit -m "your message here"
git push